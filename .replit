modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[[ports]]
localPort = 3000
externalPort = 3000

[[ports]]
npx concurrently --kill-others --prefix-colors "bgBlue.bold,bgMagenta.bold" --prefix "[{name}]" --names "BACKEND,FRONTEND" "tsx server/index.ts" "cd client && vite --host 0.0.0.0 --port 5173"localPort = 5173
cd client && npx vite --host 0.0.0.0 --port 5173externalPort = 80
